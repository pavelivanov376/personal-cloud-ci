apiVersion: v2
name: cicloud
description: Personal CI/CD learning stack — UI, api-service, gear, build-syncer, segton, Postgres, and Tekton custom resources.
type: application
version: 0.1.0
appVersion: local
